<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Use a theme attribute for flexibility -->

    <!-- Mask to define where the ripple should apply -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/_30sdp" />
            <solid android:color="@android:color/white" />
        </shape>
    </item>

    <!-- Background shape -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/_30sdp" />
            <solid android:color="@color/createAccountGray" />
            <stroke
                android:width="1dp"
                android:color="#2A3659" />
        </shape>
    </item>
</ripple>
